/**
 * Default settings, the options page may override these.
 * We keep these in a separate file so they can be access by the background
 * and option pages both.
 */

const DEFAULT_SETTINGS = {
  show_defer_post_ui: true,
};

